# Rota Admin Hub

Placeholder for the admin frontend. The Rota Core API already exposes admin data:

- `GET /admin/events` — event viewer
- `GET /admin/dashboard` — service status, top errors, slow endpoints, alerts

Planned: event replay UI, notification logs, analytics dashboard, search index
rebuild, feature flag management, workflow run viewer.
