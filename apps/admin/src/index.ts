/**
 * Rota Admin Hub — placeholder.
 *
 * Planned views (data already exposed by the API at /admin/*):
 * - Event viewer (incl. dead letter queue + replay)
 * - Notification logs
 * - Analytics dashboard (page views, top pages, DAU/WAU/MAU, funnels)
 * - Search index rebuild + popular queries
 * - Service status dashboard + alerts
 * - Feature flag management UI
 * - Workflow run viewer
 */
console.log('Rota Admin Hub: not implemented yet. See apps/api /admin/* endpoints.');
